version: '2'
services:

    dynamodb:
        environment:
                - AWS_ACCESS_KEY_ID=1234
                - AWS_SECRET_ACCESS_KEY=5678
                - AWS_REGION=us-west-2
        image: amazon/dynamodb-local
        ports:
            - 8000:8000